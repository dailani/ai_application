syntax = "proto3";

package langchain;

service LangChainService {
  // A simple API to generate a response based on a user's query
  rpc GenerateResponse (QueryRequest) returns (QueryResponse) {}
}

message QueryRequest {
  string query = 1;
}

message QueryResponse {
  string response = 1;
}
